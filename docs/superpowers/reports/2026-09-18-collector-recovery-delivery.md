# Collector recovery and a separate prospective window

September 18, 2026. Owner authorized implementation, guarded deployment and a new forward window. Demo scope, deterministic risk, `research.referee=false` and `research.handoff=false` remain unchanged. M3.1 coverage and M8.1 natural accounting remain unaccepted; checklist stays **12/55**.

## Delivered engineering

Live main advanced from `b6cc08a` to `e344a65`: `234e9b2` adds non-overlapping, namespaced population-window bindings without rewriting old bindings/receipts; `e344a65` adds scan-bound recovery health. Isolated implementation, review and tests preceded the live fast-forward. Claude CLI attempts hit its session limit; the coordinator completed the authorized slice directly, as communicated to the owner.

Health v2 binds the process instance, failure generation and highest-issued scan fence. Recovery requires two fully parent-acknowledged scan/causes pairs issued after the failure fence and at least 300 seconds since the last failure. Timeouts remain unknown even if a row reached SQLite. Error counts remain cumulative within each process; restart history is retained in the rollout evidence rather than fabricated into a new process. Producer failure signalling survives a full queue; bounded diagnostics count lost detail. Strict consumers read exactly the acknowledged scan in one read-only transaction, verify persisted identity, then re-read health. Live read clocks are sampled after reads; explicit replay clocks and registration clocks remain strict. Dashboard shares the verdict. Generation transitions have append-only gap receipts.

Population rotation refuses overlapping/rebound windows, nonempty replacement directories, pending prior exports and retroactive freezing. Old activation/binding bytes and receipt timestamps stay intact. Existing cases remain in their underlying ledgers; rotation does not backfill them into the new denominator. Closed-window captures remain possible through the archived configuration. Scheduler pass receipts (optional Slice 2) were not implemented; absent passes remain unknown.

Validation: **184 relevant tests passed**, followed by **24 recovery tests passed**, including two additional real-subprocess/gap-transition checks (**186 distinct tests**, not 208). Seven existing frozen v2 population captures replayed successfully. The preservation check verified **867 pre-existing files unchanged**, including old exports, research artifacts, doctrine, configuration and unrelated knowledge edits. [Validation](../artifacts/collector-health/2026-09-18-recovery-validation.json), [preservation/replays](../artifacts/collector-health/2026-09-18-postdeploy-preservation.json). These are engineering checks, not strategy/performance evidence. Earlier merge validation (2,015 passing, five pre-existing failures; equivalence pass, 373.9x benchmark) remains a separate historical result.

## Guarded deployment

[Pre-stop](../artifacts/collector-health/2026-09-18-immediate-prestop-runtime.json) at 02:10:19 UTC verified demo positions and all matching independent native stops, control `ACTIVE`, watchdog enabled and empty execution-recovery state (separately read). Created `watchdog.off`, sent SIGTERM to verified kernel/dashboard PIDs and waited **26.36 seconds** for exit and worker drain. [Stop barrier](../artifacts/collector-health/2026-09-18-stop-barrier.json) records no remaining module processes. Only then was main fast-forwarded and both services restarted. [Post-start](../artifacts/collector-health/2026-09-18-poststart-runtime.json) at 02:11:38 UTC confirms kernel 415868, unchanged `ACTIVE`, demo routing and all native protection. Watchdog restored immediately after this check.

Rollback was prepared and reviewed before deployment: branch `rollback/collector-recovery-20260918`, commits `956ea99` then `610ee31`; its source/test tree matches `b6cc08a`. Apply only under the same stop barrier. After window activation, also archive the current pointer and restore the prior population configuration before running old population code; retain all new exports/ledger rows and record the resulting observation interruption. Do not reset the live tree or discard unrelated files.

## Coverage and accounting limits

The original September 17→18 window remains reviewed but **coverage not accepted**. Its frozen at-cut capture is unchanged: 331 forecast and 239 investigation receipts reconciled; 31 cohort episodes, 14 resolved forecasts, 16 mature unresolved forecasts, one immature BTC investigation; missing symbols and long collection gaps remain explicit. No repaired historical sampling claim. See [window-close review](2026-09-18-m31-window-close-review.md) and [diagnosis](2026-09-18-collector-health-diagnosis.md).

The collector repair addresses permanent refusal after transient failure; it cannot ensure the kernel/host stays up or supply declared symbols absent from scans. The new declaration retains the original 16-symbol universe and coverage requirements rather than removing difficult symbols. No promised coverage acceptance at the next cut.

[Accounting refresh](../artifacts/collector-health/2026-09-18-accounting-refresh.json): no natural complete receipt. Controlled `pos_05a5e8f981` stays **complete**, seven attempts. LINK `pos_0136aef3df` and AAVE `pos_57c828f65a` remain legacy-entry receipt retries, eight attempts each; waiting cannot recreate missing historical entry evidence. NEAR/ZEC/UNI remain `waiting_close`; XRP remains an open legacy position without a queue job. No forced trade. Diagnostic-sample consumer exclusion stays owner-deferred.

## Runtime observation and next step

The **02:15 UTC natural watchdog pass** processed forecasts (`forward_scan_processed`) and both consumers accepted the same exact scan `scan_cc9ac508027d404e852a50d822170070` from instance `e9d0554c838a493fbe3c5e30c83c092c`. Investigation overall health remains `memory_source_refused`: collector acceptance does not resolve historical memory-version gaps. [Saved natural-pass preconditions](../artifacts/pit-dataset/2026-09-18-recovery-activation-preconditions.json).

A distinct declaration was frozen at **02:15:35.127 UTC** for **September 18 04:00 → September 19 04:00 UTC**, version `9b0cb68eccc51d0d4d0eca37295d9cdcd9475d5fa8927b76788b47ff66b94f41`. [Declaration](../artifacts/pit-dataset/2026-09-18-recovery-forward-declaration.json), [freeze](../artifacts/pit-dataset/2026-09-18-recovery-forward-freeze.json), [repeatable activation script](../artifacts/pit-dataset/2026-09-18-activate-recovery-window.py). The active pointer changed atomically after preserving the old configuration; original declarations/freezes were not edited. Bounded **manual consumer passes** then activated both new bindings at 02:15:51 UTC; this is separate from the preceding natural scheduled recovery proof.

[Pre-window capture](../artifacts/pit-dataset/2026-09-18-recovery-prewindow-capture.json) replays: one activation receipt per stream, no in-window population yet, `search_ready=false`. [Rollforward verification](../artifacts/pit-dataset/2026-09-18-rollforward-verification.json) preserved all original 331/239 index rows, local timestamps and binding/activation bytes. Recovery before rotation appended **post-cut** evidence to the old exports: 14 terminal + 2 missing forecast events and one investigation update (now 347/240 index/export matches). The [new old-window capture](../artifacts/pit-dataset/2026-09-18-prior-window-postrollover-capture.json) replays; these late observations do not change the frozen at-cut view or prove outcomes were known at the cut. All 867 baseline files still match.

**Additional coverage blocker confirmed after owner question.** The [current selection receipt](../artifacts/collector-health/2026-09-18-symbol-selection-evidence.json) has 20 candidates but captures only the first 16; membership lacks LSK and TAO, while SUI is present, with no frame errors for the included symbols. `kernel._scan_symbols()` follows the dynamic strategy/universe plan; `attention.capture()` truncates it to `max_symbols=16`. The frozen declaration does not reserve membership in that path. A [public no-key production probe](../artifacts/collector-health/2026-09-18-missing-symbol-public-probe.json) obtained three recent 4h candles for **all three** symbols. Thus current data availability is demonstrated; collection selection and historical provenance are separate problems. The local LSK 4h store stopped earlier than TAO/SUI. This probe does not establish why each historical omission occurred or supply earlier first-seen/version evidence.

**Exact next engineering step:** design and implement bounded collection for every declared symbol independently of trading selection, with per-symbol availability/error receipts, preserving deterministic risk and trading-universe behavior. Simply waiting, increasing a cap without guaranteed membership, or removing missing symbols from the declaration is insufficient. The new window is active but **known coverage-risk**, not a promised acceptance run. If changing collection code/protocol, preserve this declaration and freeze a distinct subsequent window after verification; do not edit this freeze or claim an unchanged protocol.

**Scheduled evidence follow-up:** after 04:00 UTC inspect in-window receipts and the outside-universe BZ investigation (supplemental only); after **12:00 UTC September 18**, inspect BTC `investigation_ec2bad86c8f4189e` in the underlying investigation ledger and write new supplemental evidence. Rotation closes old export production, so absence of new old-window export events must not be treated as absence of outcome updates. At **September 19 04:00 UTC**, complete the new full-window coverage/replay review, reconcile both indices and exports, retain missing symbols/outages/unresolved outcomes, and recheck M8.1 natural accounting. No M3.2 search or Gate 2 restart.
