# Check-now: active window, investigations, accounting (18:46 UTC)

September 18, 2026. This bounded check ran from **18:46:44 to about 18:50 UTC**, at head `51101d0`. It follows the [12:44 check](2026-09-18-check-now-1244.md). It is a snapshot. It is not coverage acceptance and not performance evidence. **Acceptance: 12/55, unchanged.** Native protection and preservation belong to the coordinator and were not repeated here.

## Session error: unintended legacy script execution, strategy state corrected

At about 18:46:55 UTC this session ran `./venv/bin/python -m scripts.validate_population --help`. The intent was to find a replay CLI. That legacy script ignores its arguments, so it **executed** the advisory TV family-proxy validation (`trader/brain/tv.py:validate_population`).

Observed effects:
- **Journal control change:** `spec_funding_filtered_trend_pullback` changed from `paper` to **`retired`**, with the reason "duplicate of auth_donchian_breakout_trail". This is a false duplicate. Both are `kind=spec` with `params={}`, so the legacy genome key collides.
- It wrote brain_events 1602–1605.
- `Vault.write_moc()` rewrote `knowledge/MOC.md` and `Donchian_Breakout_Trail.md`, both already dirty, and `Funding_Filtered_Trend_Pullback.md`, which is newly dirty and now shows retired.
- It may have sent a Telegram advisory; this is not verified.

The delegated worker stopped without attempting correction. The coordinator restored the evidenced prior `paper` state at **18:50:46.990428 UTC** using a guarded transaction that checked the exact accidental retirement first. It cleared the false duplicate reason and recorded correction event **1606**, preserving events 1602–1605. The exact prior `retire_reason` and `state_changed_at` values are unknown; the restored timestamp is the actual correction time, not a reconstructed historical value. Funding_Filtered_Trend_Pullback.md was restored to its previously clean HEAD bytes after verifying that only the two retirement labels differed. The other pre-existing dirty knowledge files were left in place. No trades for this strategy were recorded during the incident interval; missed opportunities are unknown. Notification delivery cannot be verified from the available receipt and no follow-up message was sent. [Correction receipt](../artifacts/declared-population/2026-09-18-checknow-incident-correction.json). Evidence: [incident](../artifacts/declared-population/2026-09-18-checknow-incident-validate-population-1846.json).

## Active window (Sep 18 04:00 → Sep 19 04:00 UTC, open)

[Capture](../artifacts/declared-population/2026-09-18-checknow-active-capture-1846.json) at 18:47:26–18:47:30 UTC, sha256 `7729672b…`. It replays (`replay_capture`), `window_status=open`, `search_ready=false`, `complete_sampling_claim=false`.

| Capture | Forecast idx/exp | Investigation idx/exp | Registrations | Terminal |
|---|---|---|---|---|
| 12:50 | 111/111 | 97/97 | 17 | 0 |
| **18:47** | **215/215 matched** | **170/170 matched** | **34** | **16** |

There are no missing, pending or absent-from-index exports and no orphan results. There is 1 dependence group.

- **Cohort:** 32 forecasts.
  - The 08:00 cohort has 16 rows, all **resolved** and mature (deadline 16:00).
  - The 16:00 cohort has 16 rows, all **unresolved** and immature (deadline Sep 19 00:00). AVAX registered at 16:05:08 (see below).
  - There are 2 investigations, both unresolved and immature, with all assessments indistinguishable: HYPE (deadline Sep 19 12:00) and **new BNB**, registered 16:00:11 with deadline Sep 19 16:00.
  - Selection: 26 ignored, 6 selected, 2 investigation.
- **Scans:** 164 per stream (04:00:07 → 18:45:11). `missing_symbols` is empty in every scan, and **no member was missing**. **There are no new cadence gaps since 12:50.** The only interval over 6 minutes is still 11:30→12:45, with its explicit `collection_cadence_gap` per stream.
- **Per-symbol errors** (identical on both streams; all have `retry_required`):
  - 09:00: BTC, DOGE and SOL `ValueError`.
  - 12:45: AAVE, AVAX, BNB and BR `TimeoutExpired`.
  - **New, 16:00:11: AVAX `ValueError`, 0 bars.** This caused the one forecast missing-target receipt (`exact_target_missing_retry`, AVAX 08:00 episode `033ad622…`). That forecast then resolved at 16:05:08. The cause was not diagnosed.
  - Attention `missing` is 8, matching these 3+4+1 errors.

## Underlying investigations (`data/investigation.db`, mode=ro, 18:48:37 UTC)

[Raw](../artifacts/declared-population/2026-09-18-checknow-underlying-investigations-1846.json). These are underlying-ledger reads, not old-stream exports.
- **BTC `investigation_ec2bad86c8f4189e`:** unchanged since 12:45:14. It is terminal with `update_87c9fc50a825c06b` (same/opposite contradicted, normalization compatible, next action RESEARCH). There is still **no typed_outcomes row**.
- **BZ `investigation_3864bce371354aac`:** unchanged and **not terminal**. The last update is still 04:05:07 (`exact_target_keys_missing`, WAIT). The case expires Sep 19 04:00. Its missing data remains explicit and was not repaired.

## Accounting (M8.1, queue mode=ro, 18:48 UTC)

[Check and replay](../artifacts/declared-population/2026-09-18-checknow-accounting-1846.json). The latest attempt for each job was replayed with `trade_accounting.replay`, and all passed integrity checks. **No natural complete receipt exists.** Worker health at 18:45:13 is ok: 1 complete, 3 retry, 5 waiting_close.
- Controlled SOL `pos_05a5e8f981`: still complete, 7 attempts, net −0.0659. Not reopened.
- **New retry beyond LINK/AAVE: XRP short `pos_3f5e7f308c`.** It closed at 13:54:34 on its stop fill (journal −4.60, not venue-reconciled). It has made **6 attempts**, all `legacy_entry_receipt_missing_retry`, and the next is due 20:00.
- LINK `pos_0136aef3df` has made 10 attempts (next due 23:45). AAVE `pos_57c828f65a` has made 10 (next due 01:45). Both are the same reason.
- There are 5 waiting_close jobs: NEAR, ZEC, UNI, HYPE (pending → waiting_close) and a **new SOL long `pos_051412e2d6`** opened at 16:00:56.

## Status

- There is no new cut. The current cut is **Sep 19 04:00 UTC**, and that review is still due, along with the BZ recheck at/after its expiry. The future **Sep 19 08:00 → Sep 20 08:00** window remains **inactive**.
- No cause is attributed to the 11:30–12:45 interruption. It remains unconfirmed.
- The unintended strategy retirement is corrected. Record a separate future fix for the legacy script ignoring `--help` and falsely collapsing different specs with empty genome params. Do not rerun it for inspection. The unrequested advisory evaluation is incident evidence only, not admission/performance evidence; no research gate was restarted.

## Independent runtime and preservation verification

At **18:46:46 UTC**, all five open demo positions (NEAR/ZEC/UNI/HYPE/new SOL) matched native protection; `ACTIVE`, watchdog enabled, referee/handoff false. [Runtime](../artifacts/declared-population/2026-09-18-check-now-runtime-1846.json). After the correction, `control_state=ACTIVE`, `execution_recovery=null`, and the affected strategy was read back as `paper`. [Collector/preservation snapshot](../artifacts/declared-population/2026-09-18-check-now-coordinator-1846.json) verifies the 18:45 scan has 16/16 available and the future freeze validates. Of 882 baseline paths, 881 still matched before the incident; the already-dirty Donchian note had changed since the earlier session. All compared frozen artifacts/config/exports matched. No live source, configuration, process or window rotation was performed. Claude CLI performed the bounded check; coordinator review found, disclosed and corrected the unintended mutation.
